# Frontend Environment Variables

# Backend API URL
# вљ пёЏ IMPORTANT: Leave this EMPTY for local development!
# The Vite dev server will proxy /api requests to http://localhost:8000
# Only set this for production deployment (e.g., https://api.yourdomain.com)
VITE_API_URL=

# Example for production:
# VITE_API_URL=https://api.dollarclub.com

# Google OAuth Client ID (optional - only if needed on frontend)
VITE_GOOGLE_CLIENT_ID=
